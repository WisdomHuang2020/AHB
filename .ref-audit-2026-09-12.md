# 最终合并审核报告：AHB 不对称半桥反激设计工具网站

> **审核对象**：https://wisdomhuang2020.github.io/AHB/ （AHB 不对称半桥反激设计工具）
> **审核日期**：2026-09-12
> **本地源码**：`C:\Users\xuexi\Documents\kimi\workspace\ahb-flyback`
> **审核方法**：① 逐行精读 TypeScript 源码（`src/lib/ahbMath.ts` 计算核 + 6 个页面）；② 直接调用部署 bundle 的计算核函数（`hne/w2/fne/vne/dne`）做权威复算；③ 与项目知识库 18 份源文献交叉核对；④ 与既有审阅报告（`审阅报告_AHB网站内容与计算核查_2026-09-12.md`）逐条比对裁定
> **免责标注**：本报告为 **AI 复核结论，需人工复核**（EI-1）；全部判定均附可复现证据
> **产物**：`03_中间过程数据/web_review/src-audit.js`（源码级独立复算）、`03_中间过程数据/条款裁定表_2026-09-12.csv`

---

## 一、结论摘要

**站点整体设计合理、对知识库文献的引用总体忠实，但计算核存在一条贯穿性缺陷，由它派生出三处表面独立的症状。既有审阅报告 17 条中，12 条扎实可采信、3 条部分扎实、2 条应撤回。**

| 裁定 | 条数 | 条款 |
|---|---|---|
| **A 级 · 扎实**（源码逐字证实，可直接采信） | **12** | P0-1、P0-2、P0-5、P0-7、P0-8、P1-9、P1-10、P1-12、P2-13、P2-14、P2-16，及 §五 互斥汇总表 |
| **B 级 · 部分扎实**（方向对，量级或表述需修） | **3** | P0-3、P0-6（前半）、P1-11 |
| **C 级 · 不成立**（应撤回或降级） | **2** | **P0-4**（整条撤回）、**P0-6 后半**（撤回物理断言） |
| **新增发现**（压缩代码所限，上份报告无法得出） | **4** | N-1 贯穿性根因、N-2 引注矛盾、N-3 注释与脚注冲突、N-4 版本落差 |

### 最重要的一条（新增）

**站点计算核存在单一根因，同时导致三处独立症状**：

`solveCrZcs` 收敛出的谐振电容切换电压 `v_Cr1 = 173.44 V` **被下游全部丢弃**，`resonantOffState` 与 `resonantWaveform` 一律自行重算 `D·Vin + ΔV/2 = 184.98 V`。二者相差 11.55 V，恰好**跨过副边电流的符号阈值 175.65 V**，于是：

| 副作用 | 表现 |
|---|---|
| ① 副边电流符号被迫翻转 | 站点内在同一引注「文献七式(21)」下并存 `n(i_Lm−i_Lr)` 与 `n(i_Lr−i_Lm)` 两种相反写法（→ 上一份报告 P0-5 的现象） |
| ② ZCS 调谐残差 | `i_Lr(T_off)=0.3008 A` 应等于 `I_Lm-min=−0.3676 A`，残差原边 **0.67 A** / 折算副边 **5.69 A**，却被 `max(…,0)` 显示为 0 |
| ③ 曲线页波形电荷超量 | 「精确谐振波形」隐含输出功率 **91.02 W**（设计 60 W），电荷 **1.517 倍** |

**修复优先级最高的是统一 `v_Cr1`**：一处改动可同时消除上述三项。

---

## 二、逐条裁定表

### A 级 · 扎实（可直接采信）

| 条款 | 裁定 | 源码级依据 |
|---|---|---|
| **P0-1** 两条占空比告警的 n 调整方向全反 | ✅ **扎实** | `ahbMath.ts:256-257` 原文与报告引用逐字一致；`n = dNom·vinNom/(vout+vd)`、`duty = n(vout+vd)/vin` ⇒ D ∝ n。复算 n±20% → 105.3%/70.2%、17.8%/11.9%，与报告逐位相符 |
| **P0-2** 死区告警误报且归因错误 | ✅ **扎实** | `ahbMath.ts:215` `deadtimeOk = zvsEnergyOk && deadtime >= deadtimeNeed`；`:258` ZVS 能量只判 `points.max`；`:259` 却用它弹出**死区**文案。Kr=3 复算 deadtimeNeed=294.0 ns 而输入死区 300 ns |
| **P0-5** 副边电流表达式站内互斥 | ✅ **扎实** | 见 N-2：**三处、两种相反形式、同一引注** |
| **P0-7** 二极管应力公式高估 42% | ✅ **扎实** | `ahbMath.ts:252`、`Derivations.tsx:306` 均为 `Vin_max/n + Vo`。独立推导：`D=n(Vo+Vd)/Vin` ⇒ 严格式化简为 **`V_D = Vin_max/n − Vd = 48.70 V`**，与报告的 48.70 V 两路一致；高估 42.3% |
| **P0-8** AHB 是「双向但不对称」；ACF 非「单向」 | ✅ **扎实** | 对比表 `Fundamentals.tsx:10`；「关键特性」`:91`；站内式9 `Derivations.tsx:71` `I_Lm-avg = I_o/n`；`(Kr+2)/(Kr−2)` 逐点核验通过（Kr=4→3.00、3→5.00、2.5→9.00、2.2→21.00）；`check6.js` 实测 4.12/3.00/2.79:1 |
| **P1-9** 死区谐振电感 Lm vs Lr 差 5.77 倍 | ✅ **扎实** | `ahbMath.ts:214` 用 `lm`；`Derivations.tsx:146`、`:156` 明确「由 `L_r` 与两电容串联等效决定」 |
| **P1-10** 式13 下标重建错误 | ✅ **扎实** | `Derivations.tsx:119` 逐字命中：分子 `cos(ω_{r1} D T_s)`、**分母 `sin(ω_{r2} D T_s)`**；同页式14 两个三角函数均用 ω_r1，反证分母为孤立笔误。独立复算：站点式 **821.2 V**、修正式 **165.7 V**（期望 ≈166 V） |
| **P1-12** 纹波率 0.4 vs 4 相差 10 倍 | ✅ **主结论扎实** | `Derivations.tsx:246/250/255` 取 `r = 0.4`；工具默认 `kRipple = 4`。Kr=0.4 时站点 `w2` 给出谷值 **+0.2941 A**（确实转正），ZVS 必不成立 |
| **P2-13** 「三点均满足」实为只判 2 点 | ✅ **扎实** | `ahbMath.ts:258-259` + `Designer.tsx:205` |
| **P2-14** 式27 缺 `(1−D)` | ✅ **扎实** | `Derivations.tsx:255` 显示式缺 `(1−D)`；`:257-260` 已加注；`ahbMath.ts:90` 代码**含** `(1−D)`。故错的是正文公式，实现无误 |
| **P2-16** `L_r` 硬编码 3%`L_m`、文献八 `L_r` 下限未校核 | ✅ **扎实** | `ahbMath.ts:238` `const lr = 0.03 * lm`；`AhbInputs` 无 `C_ps` 字段；`Derivations.tsx:188` 给出 `L_r` 最小值公式但无实现 |
| **§五** 站内自相矛盾汇总表 | ✅ **扎实** | 逐行可回溯至源码；唯一需注明的是末行 ZCS 判据的**根因**见 N-1 |

### B 级 · 部分扎实（方向对，量级/表述需修）

| 条款 | 裁定 | 扎实的部分 | 需修的部分 |
|---|---|---|---|
| **P0-3** 副边 RMS/峰值/输出电容纹波电流严重偏小 | ⚠️ **部分扎实** | ① 320 V 点 `iSecRms = 2.9000 < Io = 3.0000` —— **RMS 恒 ≥ \|均值\|，故数学上不可能**，此断言**铁证**；② `iCoRms = Math.sqrt(Math.max(iSecRms²−Io², 0))`（`ahbMath.ts:199`）确实被截断为 0；③ 根因确凿：`ahbMath.ts:198` 把**周期均值** `n·I_Lm,avg` 当作**导通期均值**用（正确值应为 `Io/(1−D)`） | 「严重偏小 / 偏小 2 倍」**不成立**。站点值与它**自报的 `iSecPk`** 配谐振半正弦模型仅差 **1.8%**（3.484 vs 3.548 A）。各种形状假设下估计值落在 **3.48–4.85 A**，站点值处于区间下沿而非"差 2 倍" |
| **P0-6** 图注与图数据相反（前半） | ⚠️ **前半扎实** | `Curves.tsx:56-64`：`duty` 只依赖 `v.vin`、`deltaI` 与 `pout` 无关、`iAvg ∝ pout` ⇒ 谷值随轻载**变深**；而图注写「负载减轻后谷值电流**变浅**，ZVS 逐渐丢失」→ 与数据相反，确认 | 「三处互斥」宜改为「**2 对 2**」（首页+曲线数据 vs 设计挑战+图注）。后半的物理断言见 C 级 |
| **P1-11** `Td1min/Td2min` 少系数 2 且不参与判定 | ⚠️ **部分扎实** | 「**不参与判定**」✅ 确认：`deadtimeOk` 只用 `deadtimeNeed = (π/2)√(Lm·C_eq)`，`td1min/td2min` 仅显示。「少系数 2」定性 ✅ 成立：`ahbMath.ts:218` 为 `cEq·Vin/I`，页面 `Derivations.tsx:176` 为 `2C_oss·Vin/I` | 表格数值不自洽：`Td2min` 216.9 = 2×108.5 **✓ 自洽**；但 `Td1min`「公式 69.2 ns」在**同一工作点**应为 `2cEq·320/0.9713 = **52.7 ns**`。69.2 ns 系用 420 V 的 Vin 配 320 V 的 iPk。属跨工作点取值不一致 |

### C 级 · 不成立（应撤回或降级）

| 条款 | 裁定 | 理由 |
|---|---|---|
| **P0-4** 「脚注②偏保守是错的，实际偏小 2 倍、属乐观估计」 | ❌ **整条撤回** | 其 2.05×/2.12× 的**参照基准**是曲线页 `resonantWaveform`，而该波形**自带 1.517 倍多余电荷**（隐含 91.02 W vs 设计 60 W），**不具备当基准的资格**。按电荷归一后副边 RMS 为 **4.80 A**；改用站点自报 `iSecPk` 配谐振半正弦仅 **3.48 A**（差 1.8%）。且 `ahbMath.ts:197` 代码注释写「结果**偏大**」、`Designer.tsx:185` 脚注写「偏保守」——**开发者意图一致地认为高估**。故「偏保守」无法被证伪，`偏小 2 倍`不成立。**修改建议 4 应撤回** |
| **P0-6 后半**「物理上正确的是轻载 ZVS 会丢失」 | ❌ **应撤回** | ① 与报告自己 §六 判定为 ✅ 一致的增益式 `D ≈ N·Vo/V_in` **冲突**——该式表明 D 与负载无关，故站点固定 D 扫描是合理的；② 与知识库 `an-4153cn.txt` 机理方向**相反**（「励磁电流的 DC 分量会随负载电流的减少而减少……则 ZVS 运行将由具有漏电感的励磁电感执行」，语境正是轻载不需额外 Llk）；③ 无出处。**修改建议 6 应撤回**，或仅改为「标注本图为固定 D 示意」 |
| **§六**「文献本身与图不自洽」 | ⚠️ **应降级** | 文献七为**扫描件**（`research/doc7.pdf` 6 页，文本层 0 字符），报告未提供文献七原图极性证据。可确定的仅是**站点自身对同一引注给出两个互斥形式**（N-2）。应改为「符号约定未定义 → 引注矛盾可确定，物理归属待文献原页目视核实」 |
| **本审核上一轮 A-2**（称报告修改建议 3「仍偏低 33%」） | ❌ **自我撤回** | 我上一轮同样把电荷不自洽的 7.28 A 当基准。正确关系：报告建议式 **4.85 A ≈ 电荷归一后的波形值 4.80 A**，二者**一致**。特此更正 |

---

## 三、新增发现（源码级）

### N-1 贯穿性根因：收敛 `v_Cr1` 被丢弃

| 环节 | 数值 | 源码位置 |
|---|---|---|
| `solveCrZcs` 收敛出的 `v_Cr1` | **173.44 V** | `ahbMath.ts:135-176`（`return { vCr1: v1 }`） |
| `resonantOffState` 重算 | **184.98 V** | `ahbMath.ts:107` `vCr1 = duty*vin + deltaV/2` |
| `resonantWaveform` 重算 | **184.98 V** | `ahbMath.ts:290` 同一式 |
| 被下游实际使用 | **184.98 V** | 经 bundle `fne()` 实测确认 |

**符号阈值推导**：释能初期 `i_Lr > i_Lm` 的条件为
`v_Cr1 < n·Vo + L_r·ΔI/((1−D)T_s) = 170.39 + 5.26 = **175.65 V**`

| 取值 | 与阈值关系 | 后果 |
|---|---|---|
| 求解器 **173.44 V** | **<** 175.65 | `i_Lr > i_Lm` ⇒ 副边电流为正，**符合文献七式21 `n(i_Lr−i_Lm)`** |
| 下游 **184.98 V** | **>** 175.65 | `i_Lr < i_Lm` ⇒ 按式21 将得负电流，**被迫改用 `n(i_Lm−i_Lr)`** |

→ 一条根因解释三处症状（见表见摘要）。**这是本次审核优先级最高的修复项。**

### N-2 同一引注、两种相反形式

| 位置 | 写法 | 标注 |
|---|---|---|
| `ahbMath.ts:11`（计算核文件头注释） | `i_s = n·(i_Lm − i_Lr)` | **「文献七式(21)」** |
| `Curves.tsx:145`（曲线页波形说明） | `i_s = n·(i_Lm − i_Lr)` | **「（式 21）」** |
| `Derivations.tsx:129`（推导页公式块） | `i_s(t) = n·(i_Lr(t) − i_Lm(t))` | **「文献式 21」** |

知识库文献七 OCR 原文（`cleaned/…（七）…txt` 第 89 行）：`i (t)=n(i_(Lr)(t)-i_(Lm)(t))---tag21` —— 与推导页一致。

→ 这不只是「约定未定义」，而是**站点把翻转后的形式也标注为文献七式(21)**，属引注矛盾。结合 N-1，翻转的直接动因是 `v_Cr1` 取值过高。

### N-3 代码注释与页面脚注的定性冲突

| 位置 | 原文 |
|---|---|
| `ahbMath.ts:197`（代码注释） | 「副边 RMS 按梯形波保守近似（未计入式19/21 的半正弦谐振形状，**结果偏大**，对绕组选型有利）」 |
| `Designer.tsx:185`（页面脚注②） | 「……**结果偏保守**，对绕组选型有利」 |

二者语义一致（都认为高估），说明**开发者主观意图是高估**。这从第三方向支持撤回 P0-4：真实问题不是「近似方向写反了」，而是**副边电流模型本身不可靠**（N-1 导致的 1.517 倍电荷超量）。

### N-4 部署版本落后于源码一版

| 项 | 部署站点（报告审阅对象） | 本地源码 |
|---|---|---|
| 版本 | **v1.3.0**（`Report.tsx:42` 显示 `__APP_VERSION__`） | **v1.4.0**（`package.json`） |
| 配图引用 | bundle 引用 `./fig/ahb-fig09.png` 等**位图** | `Fundamentals.tsx:28` 引用 `./fig/ahb-fig01.svg` **矢量图** |
| git 提交 | — | `3e5f4ed v1.4.0 全站12张配图手工矢量化(SVG)替换位图，标注防重叠，特性曲线页未改动` |

→ 计算类结论**不受版本落差影响**（v1.4.0 自述特性曲线页未改动，计算核 `ahbMath.ts` 未变）；但**配图类结论需在 v1.4.0 上复核**（尤其 P2-15 的「A 点」）。

---

## 四、最终应修清单（合并两份审核，按优先级）

### P0 · 必须修

1. **统一 `v_Cr1`**（N-1）：令 `resonantOffState` / `resonantWaveform` 使用 `solveCrZcs` 收敛出的 `v_Cr1`，或反过来让求解器采用 `D·Vin+ΔV/2`。**二者取一后，副边电流符号、ZCS 残差、波形电荷应一并自洽**——建议修完后跑一次电荷守恒断言（`∫i_s dt / T_s == Io`）。
2. 反转两条占空比告警的 `n` 调整方向，或改为中性表述（P0-1）。
3. 拆分 `deadtimeOk` 与 `zvsEnergyOk` 的告警文案；ZVS 能量判定改为扫**三个**工作点（P0-2）。
4. 修正副边 RMS 的导通期均值口径；`iCoRms` 增加下界校验；至少保证 `iSecRms ≥ Io`（P0-3）。
5. 统一式21 符号约定，修正引注，并在页面写明 `i_Lr`/`i_Lm` 参考方向定义（P0-5 + N-2）。
6. 二极管应力改为 `Vo + Vin_max(1−D_min)/n`（等价 `Vin_max/n − Vd = 48.70 V`）（P0-7）。
7. 「变压器励磁」整行重写：传统反激「单向励磁」；ACF 与 AHB 均改为「双向励磁（正负不对称，正向偏磁 `I_o/n`）」；删除「双向对称励磁」小节标题与「磁通在正负两个方向对称摆动」；「利用率提高一倍」改为 `ΔB·(1/2+1/Kr)`（Kr=4 时约 `0.75ΔB`）（P0-8）。

### P1 · 建议修

8. 死区谐振电感统一为 `L_r`（与文献八及等效电路图一致），或明确命名为「基于 L_m 的保守估计」（P1-9）。
9. 式13 分母改为 `sin(ω_r1·D·Ts)`（P1-10）。
10. `Td1min/Td2min` 补系数 2 或明确 `cEq ≡ 2C_oss` 的定义；纳入判定条件（P1-11）。
11. 纹波率口径统一，并说明 AHB 需负谷值；输入项改名自明（P1-12）。

### P2 · 可选

12. 曲线页负载扫描**标注为「固定 D 示意」**（**不要**按原报告建议引入随负载变 D 的模型）(P0-6)。
13. 增加 `C_ps` 输入项与文献八 `L_r` 最小值校核（P2-16）。
14. 式27 正文补 `(1−D)`（P2-14，站点已加注、代码已精确）。
15. 统一 `fig12` 的「A 点」含义（P2-15，需在 v1.4.0 上复核）。

### ❌ 应撤回的修改建议（来自既有审阅报告）

| 建议 | 处理 |
|---|---|
| 建议 4「脚注②改为偏小/乐观」 | **撤回** —— 基准波形自身电荷超量 1.517× |
| 建议 6「特性曲线页引入调压机理、固定 Vo 反解 D」 | **撤回** —— 与增益式 `D≈N·Vo/Vin`（与负载无关）冲突 |

---

## 五、局限与未核实项（如实列出）

1. **文献七/八为扫描件**：`research/doc7.pdf`（6 页）、`doc8.pdf`（3 页）文本层均为 **0 字符**。式21 的物理归属、文献七原图的 `I_d` 极性**未目视核实**，故 §六「文献本身与图不自洽」不予支持。
2. **部署版本未能从线上直接确认**：站点为 SPA，需 JS 渲染，`web_fetch` 仅能取到标题；N-4 的版本落差由 `package.json`、git 提交信息与 bundle 的配图引用形式（`.png` vs `.svg`）三路互证推得，属**强间接证据**而非直接读取。
3. **12 张插图未逐张目视**：且 v1.4.0 已替换为 SVG，配图类结论（P2-15「A 点」、图注准确性）需在 v1.4.0 上重核。
4. **副边电流真值不可唯一确定**：本文给出 3.48–4.85 A 的模型区间而非单一数，因真实波形形状（半正弦/三角/梯形）在站点模型中未自洽定义（N-1）。**唯有 `iSecRms ≥ Io` 是不可辩驳的硬约束。**
5. **本报告是审核报告，不是系统综述**；证据级别为**一手可复现核验**（源码逐行 + bundle 计算核心调用 + 知识库交叉），覆盖范围为两份审阅所涉全部可验证条款。

---

## 六、溯源附录

| 证据 | 位置 |
|---|---|
| 站点计算核（可读 TS） | `kimi/workspace/ahb-flyback/src/lib/ahbMath.ts` |
| 各页面源码 | `src/pages/{Fundamentals,Derivations,Designer,Curves,Report,Operation,Home}.tsx` |
| 部署 bundle（与报告同源） | `03_中间过程数据/web_review/assets/index-CwZD5lSL.js` |
| 源码级独立复算脚本 | `03_中间过程数据/web_review/src-audit.js` |
| 既有复算脚本（复核后复用） | `03_中间过程数据/web_review/check2..6.js` |
| 知识库源文献 | `02_RAG知识库/cleaned/`（文献二/七/八、AN-4153、廖鸿飞等） |
| 知识库励磁独立路线 | `05_工具脚本/诊断检查/compute_excitation_symmetry.py` |
| 本报告条款裁定表（机器可读） | `03_中间过程数据/条款裁定表_2026-09-12.csv` |

*报告结束。第三节全部数值可用 `node src-audit.js` 复现；第二节 A 级条款的证据均在源码行号处可直接核对。*
